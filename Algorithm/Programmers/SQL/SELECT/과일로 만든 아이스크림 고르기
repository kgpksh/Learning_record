-- 코드를 입력하세요
SELECT FLAVOR FROM FIRST_HALF AS FH
WHERE TOTAL_ORDER > 3000 AND 
    (SELECT INGREDIENT_TYPE FROM ICECREAM_INFO AS II
    WHERE II.FLAVOR = FH.FLAVOR) = 'fruit_based'
ORDER BY TOTAL_ORDER DESC